mod global;

extern crate nalgebra_glm as glm;

const WIDTH   :u32 = 120u32;
const HEIGHT  :u32 = 120u32;

fn main() {
    // Scene scene(WIDTH, HEIGHT);

    // let sph1 = Box<Sphere>(glm::Vec3(-1., 0., -12), 2.);
    // sph1
}
